process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    ext.args = ""

}

plugins {
    id 'nf-validation@0.1.0'
}

params {

    sample          = null
    file_list       = null
    args            = null

}
